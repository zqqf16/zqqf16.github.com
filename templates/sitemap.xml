<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
% for page in pages:
	<url>
		<loc>http://${info['domain']}/${page.uri}</loc>
		<lastmod>${page.date.strftime('%Y-%m-%d')}</lastmod>
		<changefreq>monthly</changefreq>
	</url>
% endfor
% for post in posts:
	<url>
		<loc>http://${info['domain']}/${post.uri}</loc>
		<lastmod>${post.date.strftime('%Y-%m-%d')}</lastmod>
		<changefreq>monthly</changefreq>
	</url>
% endfor
</urlset>
